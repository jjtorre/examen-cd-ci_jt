<<<<<<< HEAD
name: CD - Deploy API
=======
name: Deploy to Render
>>>>>>> cffa74c94f4c5fc268fac9c71923d91231bf4290

on:
  push:
    tags:
<<<<<<< HEAD
      - 'v*'  # Se ejecuta cuando se crea un tag que empiece con v

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest

      - name: Deploy to Render
        env:
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        run: |
          echo "Ejecutando deploy a Render..."
          curl -X POST https://api.render.com/deploy/srv-d4t3174hg0os73cka8e0?key=${{ secrets.RENDER_API_KEY }}
=======
      - "v*"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del código
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Configurar variables de entorno de producción
        run: |
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" >> $GITHUB_ENV
          echo "DB_URL=${{ secrets.DB_URL }}" >> $GITHUB_ENV

      - name: Trigger deploy en Render
        env:
          RENDER_SERVICE_ID: ${{ secrets.RENDER_SERVICE_ID }}
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        run: |
          curl -X POST \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": false}' \
            https://api.render.com/v1/services/$RENDER_SERVICE_ID/deploys

      - name: Confirmar deploy
        run: |
          echo "🚀 Deploy enviado correctamente a Render"
>>>>>>> cffa74c94f4c5fc268fac9c71923d91231bf4290
